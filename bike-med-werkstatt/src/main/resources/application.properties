server.port=5005
spring.rabbitmq.address=${AMQP_URL}

queue.dispatcher.name=bike-med-dispatcher-queue
queue.api.name=bike-med-api-queue
queue.werkstatt.name=bike-med-werkstatt-queue
queue.lager.name=bike-med-lager-queue
queue.office.name=bike-med-invoicing-queue

# Database
# ------------------------------------------------------------------------------
spring.datasource.url=jdbc:postgresql://localhost:5432/werkstatt
spring.datasource.username=bikemed
spring.datasource.password=bikemed
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Hibernate
# ------------------------------------------------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
